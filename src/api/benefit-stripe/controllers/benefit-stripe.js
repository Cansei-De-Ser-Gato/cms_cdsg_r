'use strict';

/**
 * benefit-stripe controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::benefit-stripe.benefit-stripe');
